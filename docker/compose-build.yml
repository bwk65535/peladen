version: "3"
services:
  jekyll:
    build:
      context: ../.
      dockerfile: docker/build.Dockerfile
    image: v0latile/peladen:latest
    networks:
      - peladen-net
    ports:
      - "8080:80"

networks:
  peladen-net: