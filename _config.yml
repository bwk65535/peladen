title: Peladen Blog
email: medullium@yandex.com
description: >- # this means to ignore newlines until "baseurl:"
  semua tentang jaringan komputer
# baseurl: "" # the subpath of your site, e.g. /blog
url: "https://peladen.netlify.app"

show_excerpts: true
paginate: 5
paginate_path: "/page/:num/"

twitter_username: v0latile
# github_username:  jekyll

# Build settings
theme: minima
plugins:
  - jekyll-feed
  - jekyll-sitemap
  - jekyll-seo-tag
  - jekyll-paginate

# Exclude from processing.
exclude:
  - .sass-cache/
  - .jekyll-cache/
  - gemfiles/
  - Gemfile
  - Gemfile.lock
  - node_modules/
  - vendor/bundle/
  - vendor/cache/
  - vendor/gems/
  - vendor/ruby/